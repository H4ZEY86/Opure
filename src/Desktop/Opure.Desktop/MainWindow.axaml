<Window
    x:Class="Opure.Desktop.MainWindow"
    x:DataType="contracts:DesktopShellViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:contracts="using:Opure.Desktop.Contracts"
    mc:Ignorable="d"
    d:DesignWidth="1000"
    d:DesignHeight="650"
    Width="1000"
    Height="650"
    MinWidth="760"
    MinHeight="500"
    WindowStartupLocation="CenterScreen"
    Title="{Binding WindowTitle}"
    AutomationProperties.Name="Opure Desktop main window"
    AutomationProperties.AutomationId="OpureMainWindow">

  <Grid
      RowDefinitions="Auto,*,Auto"
      ColumnDefinitions="220,*">

    <Border
        Grid.Row="0"
        Grid.ColumnSpan="2"
        Padding="24,16">
      <StackPanel Spacing="4">
        <TextBlock
            Text="{Binding ProductHeading}"
            FontSize="28"
            FontWeight="SemiBold"
            AutomationProperties.Name="Opure"
            AutomationProperties.HeadingLevel="1" />
        <TextBlock
            Text="{Binding Motto}"
            TextWrapping="Wrap"
            AutomationProperties.Name="Developer Respect. Local Intelligence. Complete Control." />
      </StackPanel>
    </Border>

    <Border
        Grid.Row="1"
        Grid.Column="0"
        Padding="16">
      <StackPanel Spacing="8">
        <TextBlock
            Text="Navigation"
            FontSize="18"
            FontWeight="SemiBold"
            AutomationProperties.HeadingLevel="2" />

        <Button
            x:Name="HomeButton"
            Content="_Home"
            Click="OnHomeClick"
            IsTabStop="True"
            TabIndex="1"
            AutomationProperties.Name="Home"
            AutomationProperties.AutomationId="NavigationHome"
            AutomationProperties.AccessKey="Alt+H" />

        <Button
            x:Name="ProjectsButton"
            Content="_Projects"
            Click="OnProjectsClick"
            IsTabStop="True"
            TabIndex="2"
            AutomationProperties.Name="Projects"
            AutomationProperties.AutomationId="NavigationProjects"
            AutomationProperties.AccessKey="Alt+P" />

        <Button
            x:Name="WorkflowsButton"
            Content="_Workflows"
            Click="OnWorkflowsClick"
            IsTabStop="True"
            TabIndex="3"
            AutomationProperties.Name="Workflows"
            AutomationProperties.AutomationId="NavigationWorkflows"
            AutomationProperties.AccessKey="Alt+W" />

        <Button
            x:Name="TrustCentreButton"
            Content="_Trust Centre"
            Click="OnTrustCentreClick"
            IsTabStop="True"
            TabIndex="4"
            AutomationProperties.Name="Trust Centre"
            AutomationProperties.AutomationId="NavigationTrustCentre"
            AutomationProperties.AccessKey="Alt+T" />
      </StackPanel>
    </Border>

    <ScrollViewer
        Grid.Row="1"
        Grid.Column="1"
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto">
      <StackPanel
          Margin="32"
          Spacing="20">

        <TextBlock
            x:Name="PageTitleText"
            Text="{Binding PageTitle}"
            FontSize="24"
            FontWeight="SemiBold"
            AutomationProperties.HeadingLevel="1" />

        <TextBlock
            x:Name="PageDetailText"
            Text="{Binding PageDetail}"
            TextWrapping="Wrap" />

        <GroupBox
            Header="Registered projects"
            IsVisible="{Binding IsProjectsPage}"
            AutomationProperties.Name="Registered projects from Project Service"
            AutomationProperties.AutomationId="RegisteredProjectListGroup">
          <Grid Margin="8" RowDefinitions="Auto,Auto,*,Auto">
            <StackPanel Spacing="4">
              <TextBlock
                  Text="{Binding ProjectList.StatusTitle}"
                  FontWeight="SemiBold"
                  AutomationProperties.Name="{Binding ProjectList.StatusTitle}"
                  AutomationProperties.AutomationId="ProjectListStatusTitle" />
              <TextBlock
                  Text="{Binding ProjectList.StatusDetail}"
                  TextWrapping="Wrap"
                  AutomationProperties.Name="{Binding ProjectList.StatusDetail}"
                  AutomationProperties.AutomationId="ProjectListStatusDetail" />
            </StackPanel>
            <ProgressBar
                Grid.Row="1"
                Margin="0,8,0,0"
                IsIndeterminate="True"
                IsVisible="{Binding ProjectList.IsBusy}"
                AutomationProperties.Name="Refreshing registered projects" />
            <ListBox
                x:Name="RegisteredProjectList"
                Grid.Row="2"
                Margin="0,8"
                MinHeight="96"
                MaxHeight="280"
                ItemsSource="{Binding ProjectList.Projects}"
                SelectedItem="{Binding ProjectList.SelectedProject, Mode=TwoWay}"
                IsVisible="{Binding ProjectList.HasProjects}"
                SelectionMode="Single"
                TabIndex="5"
                AutomationProperties.Name="Registered projects. Use arrow keys to select a project."
                AutomationProperties.AutomationId="RegisteredProjectList">
              <ListBox.ItemTemplate>
                <DataTemplate x:DataType="contracts:DesktopProjectListItem">
                  <Grid
                      Margin="8,6"
                      ColumnDefinitions="2*,Auto"
                      RowDefinitions="Auto,Auto,Auto"
                      AutomationProperties.Name="{Binding AccessibilityLabel}">
                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                    <TextBlock Grid.Column="1" Margin="16,0,0,0" Text="{Binding AvailabilityLabel}" FontWeight="SemiBold" />
                    <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding SafeLocationSummary}" />
                    <TextBlock Grid.Row="2" Text="{Binding RepositoryClass}" />
                    <TextBlock Grid.Row="2" Grid.Column="1" Margin="16,0,0,0" Text="{Binding LastOpenedLabel}" />
                  </Grid>
                </DataTemplate>
              </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="8">
              <Button
                  x:Name="OpenRegisteredProjectButton"
                  Content="_Open selected"
                  Click="OnOpenRegisteredProjectClick"
                  IsEnabled="{Binding ProjectList.CanOpen}"
                  TabIndex="6"
                  AutomationProperties.Name="Open selected registered project through Project Service"
                  AutomationProperties.AutomationId="OpenRegisteredProject"
                  AutomationProperties.AccessKey="Alt+O" />
              <Button
                  x:Name="RemoveProjectRegistrationButton"
                  Content="_Remove registration"
                  Click="OnRemoveProjectRegistrationClick"
                  IsEnabled="{Binding ProjectList.CanRemoveRegistration}"
                  TabIndex="7"
                  AutomationProperties.Name="Remove selected project registration only. Project files will not be deleted."
                  AutomationProperties.AutomationId="RemoveProjectRegistration"
                  AutomationProperties.AccessKey="Alt+M" />
              <Button
                  x:Name="RefreshProjectListButton"
                  Content="Re_fresh list"
                  Click="OnRefreshProjectListClick"
                  IsEnabled="{Binding ProjectList.CanRefresh}"
                  TabIndex="8"
                  AutomationProperties.Name="Refresh registered projects from Project Service"
                  AutomationProperties.AutomationId="RefreshProjectList"
                  AutomationProperties.AccessKey="Alt+F" />
            </StackPanel>
          </Grid>
        </GroupBox>

        <GroupBox
            Header="Open project"
            IsVisible="{Binding IsProjectsPage}"
            AutomationProperties.Name="Open a verified project folder"
            AutomationProperties.AutomationId="ProjectFolderPicker">
          <StackPanel Margin="8" Spacing="8">
            <Button
                x:Name="SelectProjectFolderButton"
                Content="_Select folder"
                Click="OnSelectProjectFolderClick"
                IsEnabled="{Binding ProjectFolderPicker.CanSelect}"
                IsTabStop="True"
                TabIndex="9"
                HorizontalAlignment="Left"
                AutomationProperties.Name="Select project folder"
                AutomationProperties.AutomationId="SelectProjectFolder"
                AutomationProperties.AccessKey="Alt+S" />
            <ProgressBar
                IsIndeterminate="True"
                IsVisible="{Binding ProjectFolderPicker.IsSelecting}"
                AutomationProperties.Name="Verifying selected project folder" />
            <TextBlock
                Text="{Binding ProjectFolderPicker.SelectedPathDisplay}"
                TextWrapping="Wrap"
                AutomationProperties.Name="{Binding ProjectFolderPicker.SelectedPathDisplay}"
                AutomationProperties.AutomationId="SelectedProjectPathDisplay" />
            <TextBlock
                Text="{Binding ProjectFolderPicker.Classification}"
                FontWeight="SemiBold"
                TextWrapping="Wrap"
                AutomationProperties.Name="{Binding ProjectFolderPicker.Classification}"
                AutomationProperties.AutomationId="SelectedProjectPathClassification" />
            <TextBlock
                Text="{Binding ProjectFolderPicker.SafeDetail}"
                TextWrapping="Wrap"
                AutomationProperties.Name="{Binding ProjectFolderPicker.SafeDetail}"
                AutomationProperties.AutomationId="SelectedProjectPathDetail" />
          </StackPanel>
        </GroupBox>

        <Border
            x:Name="SafeModeBanner"
            IsVisible="{Binding RuntimeHealth.IsSafeMode}"
            BorderThickness="3"
            CornerRadius="4"
            Padding="16"
            AutomationProperties.Name="Safe Mode. Optional components are restricted."
            AutomationProperties.AutomationId="RuntimeSafeModeBanner">
          <StackPanel Spacing="4">
            <TextBlock
                Text="SAFE MODE"
                FontSize="22"
                FontWeight="Bold"
                AutomationProperties.HeadingLevel="1" />
            <TextBlock
                Text="Optional components are restricted. Review Runtime recovery before resuming normal operation."
                TextWrapping="Wrap" />
          </StackPanel>
        </Border>

        <GroupBox
            Header="Runtime Health"
            AutomationProperties.Name="{Binding RuntimeHealth.AccessibilitySummary}"
            AutomationProperties.AutomationId="RuntimeHealthStatus">
          <StackPanel
              Margin="8"
              Spacing="12">
            <Grid ColumnDefinitions="*,Auto">
              <StackPanel Spacing="4">
                <TextBlock
                    x:Name="RuntimeStatusText"
                    Text="{Binding RuntimeHealth.StatusTitle}"
                    FontSize="20"
                    FontWeight="SemiBold"
                    AutomationProperties.Name="{Binding RuntimeHealth.StatusTitle}"
                    AutomationProperties.AutomationId="RuntimeStatusTitle"
                    AutomationProperties.HeadingLevel="2" />
                <TextBlock
                    x:Name="RuntimeStatusDetailText"
                    Text="{Binding RuntimeHealth.StatusDetail}"
                    TextWrapping="Wrap"
                    AutomationProperties.Name="{Binding RuntimeHealth.StatusDetail}"
                    AutomationProperties.AutomationId="RuntimeStatusDetail" />
                <TextBlock
                    x:Name="ProjectionFreshnessText"
                    Text="{Binding RuntimeHealth.ProjectionFreshnessLabel}"
                    FontWeight="SemiBold"
                    AutomationProperties.Name="{Binding RuntimeHealth.ProjectionFreshnessLabel}"
                    AutomationProperties.AutomationId="RuntimeProjectionFreshness" />
              </StackPanel>

              <Button
                  x:Name="RefreshRuntimeButton"
                  Grid.Column="1"
                  Margin="16,0,0,0"
                  Content="_Refresh Runtime"
                  Click="OnRefreshRuntimeClick"
                  IsEnabled="{Binding RuntimeHealth.CanRefresh}"
                  IsTabStop="True"
                  TabIndex="10"
                  AutomationProperties.Name="Refresh Runtime Health"
                  AutomationProperties.AutomationId="RefreshRuntimeHealth"
                  AutomationProperties.AccessKey="Alt+R" />
            </Grid>

            <ProgressBar
                x:Name="RuntimeRefreshProgress"
                IsIndeterminate="True"
                IsVisible="{Binding RuntimeHealth.IsRefreshing}"
                AutomationProperties.Name="Refreshing Runtime Health"
                AutomationProperties.AutomationId="RuntimeRefreshProgress" />

            <Border
                x:Name="DegradedRuntimeBanner"
                IsVisible="{Binding RuntimeHealth.IsDegraded}"
                BorderThickness="2"
                CornerRadius="4"
                Padding="12"
                AutomationProperties.Name="Runtime degraded. Reduced capability is visible in the service list."
                AutomationProperties.AutomationId="RuntimeDegradedBanner">
              <TextBlock
                  Text="Degraded — the Runtime is available with reduced capability. This state is not healthy."
                  FontWeight="SemiBold"
                  TextWrapping="Wrap" />
            </Border>

            <Grid
                IsVisible="{Binding RuntimeHealth.HasBootIdentity}"
                ColumnDefinitions="Auto,*,Auto">
              <TextBlock
                  Text="Boot identity"
                  VerticalAlignment="Center"
                  FontWeight="SemiBold" />
              <TextBlock
                  x:Name="RuntimeBootIdentityShortText"
                  Grid.Column="1"
                  Margin="12,0"
                  Text="{Binding RuntimeHealth.RuntimeBootIdShort}"
                  VerticalAlignment="Center"
                  AutomationProperties.Name="{Binding RuntimeHealth.RuntimeBootId}"
                  AutomationProperties.AutomationId="RuntimeBootIdentity" />
              <Button
                  x:Name="CopyRuntimeBootIdentityButton"
                  Grid.Column="2"
                  Content="_Copy boot identity"
                  Click="OnCopyRuntimeBootIdentityClick"
                  IsEnabled="{Binding RuntimeHealth.CanCopyBootIdentity}"
                  IsTabStop="True"
                  TabIndex="11"
                  AutomationProperties.Name="Copy Runtime boot identity"
                  AutomationProperties.AutomationId="CopyRuntimeBootIdentity"
                  AutomationProperties.AccessKey="Alt+C" />
            </Grid>

            <GroupBox
                Header="Services"
                AutomationProperties.Name="Runtime service health list"
                AutomationProperties.AutomationId="RuntimeServiceHealthGroup">
              <Grid RowDefinitions="Auto,*">
                <TextBlock
                    IsVisible="{Binding !RuntimeHealth.HasServices}"
                    Margin="8"
                    Text="No service projection is available."
                    TextWrapping="Wrap" />
                <ListBox
                    x:Name="ServiceHealthList"
                    Grid.Row="1"
                    MaxHeight="260"
                    ItemsSource="{Binding RuntimeHealth.Services}"
                    SelectionMode="Single"
                    TabIndex="12"
                    AutomationProperties.Name="Runtime services. Use arrow keys to inspect each row."
                    AutomationProperties.AutomationId="RuntimeServiceHealthList">
                  <ListBox.ItemTemplate>
                    <DataTemplate x:DataType="contracts:DesktopServiceHealthRow">
                      <Grid
                          Margin="8,6"
                          ColumnDefinitions="2*,Auto"
                          RowDefinitions="Auto,Auto,Auto">
                        <TextBlock
                            Text="{Binding ServiceId}"
                            FontWeight="SemiBold"
                            AutomationProperties.Name="{Binding AccessibilityLabel}"
                            AutomationProperties.AutomationId="{Binding AutomationId}" />
                        <TextBlock
                            Grid.Column="1"
                            Margin="16,0,0,0"
                            Text="{Binding StateLabel}"
                            FontWeight="SemiBold" />
                        <TextBlock
                            Grid.Row="1"
                            Grid.ColumnSpan="2"
                            Text="{Binding SafeDetail}"
                            TextWrapping="Wrap" />
                        <TextBlock
                            Grid.Row="2"
                            Grid.ColumnSpan="2"
                            IsVisible="{Binding HasStableFailureCode}"
                            Text="{Binding StableFailureCode, StringFormat='Stable error code: {0}'}"
                            TextWrapping="Wrap" />
                      </Grid>
                    </DataTemplate>
                  </ListBox.ItemTemplate>
                </ListBox>
              </Grid>
            </GroupBox>

            <Expander
                x:Name="RuntimeDetailsExpander"
                Header="Runtime details"
                IsExpanded="False"
                TabIndex="13"
                AutomationProperties.Name="Runtime diagnostic details"
                AutomationProperties.AutomationId="RuntimeDetailsDisclosure">
              <Grid
                  Margin="8"
                  ColumnDefinitions="Auto,*"
                  RowDefinitions="Auto,Auto,Auto,Auto">
                <TextBlock Text="Full boot identity" FontWeight="SemiBold" />
                <TextBlock
                    Grid.Column="1"
                    Margin="12,0,0,0"
                    Text="{Binding RuntimeHealth.RuntimeBootId}"
                    TextWrapping="Wrap" />
                <TextBlock Grid.Row="1" Text="Runtime version" FontWeight="SemiBold" />
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="12,0,0,0"
                    Text="{Binding RuntimeHealth.RuntimeProductVersion}"
                    TextWrapping="Wrap" />
                <TextBlock Grid.Row="2" Text="Stable error code" FontWeight="SemiBold" />
                <TextBlock
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="12,0,0,0"
                    Text="{Binding RuntimeHealth.StableErrorCodeDisplay}"
                    TextWrapping="Wrap" />
                <TextBlock Grid.Row="3" Text="Projection timestamp" FontWeight="SemiBold" />
                <TextBlock
                    Grid.Row="3"
                    Grid.Column="1"
                    Margin="12,0,0,0"
                    Text="{Binding RuntimeHealth.GeneratedUnixTimeMilliseconds}"
                    TextWrapping="Wrap" />
              </Grid>
            </Expander>
          </StackPanel>
        </GroupBox>
      </StackPanel>
    </ScrollViewer>

    <Border
        Grid.Row="2"
        Grid.ColumnSpan="2"
        Padding="16,10">
      <Grid ColumnDefinitions="*,Auto">
        <TextBlock
            x:Name="StatusBarText"
            Text="{Binding RuntimeHealth.StatusBarText}"
            TextWrapping="Wrap"
            AutomationProperties.Name="{Binding RuntimeHealth.StatusBarText}"
            AutomationProperties.AutomationId="DesktopStatusBar" />
        <TextBlock
            Grid.Column="1"
            Margin="16,0,0,0"
            Text="{Binding ProductVersion}"
            AutomationProperties.Name="Desktop product version"
            AutomationProperties.AutomationId="DesktopProductVersion" />
      </Grid>
    </Border>
  </Grid>
</Window>
