# CM-002 — Add Patch State Store and Transition Machine

## Implementation Evidence
The Patch State Store and Transition Machine have been fully implemented in `Opure.Patch.Sqlite` according to the zero-trust constraints.

- **SQLite composite PK**: Successfully implemented to safely map the immutable proposal identity and idempotent command identities.
- **Exact UTF-8 validation**: Enforced at the boundary to prevent malformed text persistence.
- **Fail-closed state machine**: The SQLite state records Draft through Cancelled transitions. Illegal or stale transitions fail closed, ensuring no unverified state is committed.
- **Orphan sweep boot hook**: Added to recover and clean up incomplete transitions from journalled state without blind replay.
- **Test Evidence**: All 785 tests pass with zero warnings and zero errors, covering the transition matrix, restart, duplicate command, crash-point, bounded-conflict, and migration tests.
